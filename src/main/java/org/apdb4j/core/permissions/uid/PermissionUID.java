package org.apdb4j.core.permissions.uid;

import org.apdb4j.core.permissions.Access;

/**
 * Represents an UID (Unique ID) for a set of access settings (permission).
 * @see Access
 */
public interface PermissionUID {
}
